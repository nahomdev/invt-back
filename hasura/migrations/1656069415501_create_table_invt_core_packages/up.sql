CREATE EXTENSION IF NOT EXISTS pgcrypto;
CREATE TABLE "invt_core"."packages"("id" uuid NOT NULL DEFAULT gen_random_uuid(), "billing_address" uuid NOT NULL, "contact_persons" uuid NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "customer_id" uuid NOT NULL, "date" date NOT NULL, "email" text NOT NULL, "is_emailed" boolean NOT NULL, "last_modified_time" date NOT NULL, "line_items" text NOT NULL, "mobile" text NOT NULL, "notes" text NOT NULL, "package_number" text NOT NULL, "phone" text NOT NULL, "salesorder_id" text NOT NULL, "shipping_address" uuid NOT NULL, "total_quantity" text NOT NULL, PRIMARY KEY ("id") );
